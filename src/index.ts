
const x="Madame Jane Doe née le 3 avril 2010  "

const x2="Madame Jane Madame Jane Doe née le 3 avril 2010  "

//const regex=/[1-31] (.+) \d{4} /igm

const liste=[['a'],['b'],['c']];

console.log('test');

/* const inp=document.querySelector('input')


const onChange=(e:Event)=> {
   
    const currentElement=e.currentTarget as HTMLInputElement
    
    if(currentElement.files) {
        const files=currentElement.files[0]

        console.log(files.type);
        
        const reader=new FileReader()

        reader.readAsDataURL(files)

        if(files.type==='application/pdf') {

            reader.addEventListener('load',(e)=> {
                const a=document.querySelector('a')
                
                a?.setAttribute('href',`${reader.result}`)
                console.log(a);
            })

        }else {
            reader.addEventListener('load',(e)=> {
                const img=document.querySelector('img')
                console.log(e.type);
                console.log(reader);
                img?.setAttribute('src',`${reader.result}`)
            })
        }

        
    }
}

inp?.addEventListener('change',onChange) */

